var highschool = ['品品', '慶生', '暉暉', '子雞', '大嫂'];
var others = ['抽', '幹嘛', '哈哈', '指令'];
var pix = [];

var all = highschool.concat(others).join(' ');
module.exports = {
    highschool,
    others,
    all
}